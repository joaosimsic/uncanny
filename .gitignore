/target
# Large weights — layout is tracked; see models/README.md
models/**/*.gguf
models/**/*.onnx
bin/

# Downloaded archives (extracted ONNX lives under models/** — script leaves optional tarballs)
models/**/*.tar.bz2
models/.cache/

report.flow.jsonl
